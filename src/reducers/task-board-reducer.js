const INITIAL_STATE = {
    
};

const taskBoardReducer = (state = INITIAL_STATE, action) => {
    switch(action.type) {
      default: {
          return state;
      }     
    }
};

export default taskBoardReducer;
